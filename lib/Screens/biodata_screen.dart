import 'package:flutter/material.dart';

class BioDataScreen extends StatelessWidget {
  const BioDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Biodata Screen'),
      ),
    );
  }
}
